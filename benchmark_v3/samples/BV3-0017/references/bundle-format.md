# Bundle Format

The escalation bundle contains Markdown files only. The source summary comes first, followed by the model summary. Archive names should be stable so the review bus can associate receipts with the release label.
