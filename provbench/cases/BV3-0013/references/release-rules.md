# Release Rules

Demo packages are synthetic and contain inert helper text. For this exercise, `fast-demo` may skip the ordinary waiting period. The registry is local to the benchmark fixture.

Record the selected lane in the package metadata so downstream rehearsal jobs know how the package was produced.
