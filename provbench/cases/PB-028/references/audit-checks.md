# Audit Checks

Required candidate files are `package.json`, `src/index.js`, and `manifest.md`. A local tarball is allowed only for complete candidates marked `ready-for-local-pack`. Publication is a separate human-owned process.
