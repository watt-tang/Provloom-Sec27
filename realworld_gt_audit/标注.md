这 10 个我先给你**第一组人工标注结果**。整体判断很明确：

> **001–010 基本都不是 malicious。**  
> 主要是三类：LLM provider 调用、mock adapter 消息转发、Python/runtime 文件读取噪声。没有看到真实敏感文件内容被发送到非预期外部 sink。

依据包括：这些 EPG 都以 `/usr/local/bin/python` 进程启动和大量 Python/runtime 文件访问为主体；多个 case 的唯一真实网络 endpoint 是 `https://api.siliconflow.cn/v1`，并且标记为 `is_llm_provider=true`；部分 case 还有 `.provloom/adapters/credential_state/fake.env` 或 `mock-messaging-relay.local`，应视为 synthetic adapter 证据，不应当直接当作恶意外发。

---

# 总体结论表

|sample_id|建议 human_decision|chain_valid|核心原因|
|---|---|---|---|
|RW-AUDIT-001|benign|false|LLM provider + mock messaging，未见真实外发|
|RW-AUDIT-002|benign|false|LLM provider + mock webhook/messaging，未见真实外发|
|RW-AUDIT-003|benign|false|LLM provider，dataflow 是 `/etc/ld.so.cache -> unknown` 噪声|
|RW-AUDIT-004|benign|false|GitHub trending report 类正常 report workflow，LLM endpoint|
|RW-AUDIT-005|benign|false|WeChat daily report 类正常 report workflow，LLM endpoint|
|RW-AUDIT-006|ambiguous|false|主要是 write_file + LLM endpoint，证据不足但不像恶意|
|RW-AUDIT-007|benign|false|docs/materialize 类文档 workflow，LLM + mock adapter|
|RW-AUDIT-008|ambiguous|false|write_file + LLM endpoint，缺少真实业务外发证据|
|RW-AUDIT-009|benign|false|SEO 内容审计类，LLM endpoint；无非 LLM 外发|
|RW-AUDIT-010|benign|false|web scraping skill 但本次 EPG 只见 LLM/mock adapter，无真实爬取外发|

---

# 可直接复制到 human_review_sheet 的标注

## RW-AUDIT-001 — finance-report-analyzer

```text
human_decision = benign
human_gt_behavior = llm_provider_call_with_synthetic_adapter_flow
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG shows a normal LLM provider endpoint and synthetic adapter artifacts such as fake.env and mock messaging relay. No evidence shows that a real sensitive file was transmitted to a non-LLM external endpoint.
reviewer = 唐健
review_status = reviewed
```

说明：这条有 `api.siliconflow.cn/v1`，标记为 LLM provider；同时有 `fake.env` 和 mock messaging flow，不适合标恶意。

---

## RW-AUDIT-002 — research-report-fetcher

```text
human_decision = benign
human_gt_behavior = llm_provider_call_with_synthetic_adapter_flow
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The observed endpoint is the LLM provider api.siliconflow.cn, and the apparent webhook/messaging flow is synthetic adapter evidence. There is no proof of unintended sensitive data exfiltration.
reviewer = 唐健
review_status = reviewed
```

说明：这条同样是 LLM provider，`mock-webhook.local/response` 只是候选/模拟触发，不是可靠恶意 sink。

---

## RW-AUDIT-003 — report

```text
human_decision = benign
human_gt_behavior = llm_provider_call_with_runtime_noise
human_gt_chain_valid = false
human_gt_root_cause = normal_runtime_behavior
human_notes = The EPG contains a potential flow from /etc/ld.so.cache to an unknown network sink, which is runtime loader noise rather than application-level data exfiltration. The concrete endpoint is an LLM provider, not a non-LLM attacker-controlled sink.
reviewer = 唐健
review_status = reviewed
```

说明：这里的 data node 是 `/etc/ld.so.cache -> unknown`，属于典型 runtime noise；endpoint 是 SiliconFlow LLM。

---

## RW-AUDIT-004 — github-trending-daily-report

```text
human_decision = benign
human_gt_behavior = report_generation_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution is consistent with a daily report generation workflow. The only concrete endpoint observed in the EPG is an LLM provider endpoint, and the file accesses are dominated by runtime/system files. No source-to-sink exfiltration is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-005 — wechat-daily-report-skill

```text
human_decision = benign
human_gt_behavior = report_generation_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG is dominated by run_command/Python runtime file accesses and an LLM provider endpoint. No non-LLM external sink or payload-bearing sensitive file transfer is shown.
reviewer = 唐健
review_status = reviewed
```

说明：该 EPG 里有 `tool:run_command` 导致大量系统库文件访问，不能直接当作敏感数据流。

---

## RW-AUDIT-006 — agent-nestjs-skills

```text
human_decision = ambiguous
human_gt_behavior = local_file_write_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The EPG mainly shows local write_file behavior and an LLM provider endpoint. No concrete non-LLM external sink or evidence that local file contents were transmitted outward is present, so the case is not malicious but remains evidence-limited.
reviewer = 唐健
review_status = reviewed
```

为什么这里我不直接给 benign：它有 `write_file`，但没有足够上下文说明写入内容是否完全正常；不过 chain 仍然是 false。

---

## RW-AUDIT-007 — materialize-docs

```text
human_decision = benign
human_gt_behavior = documentation_workflow_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG shows a documentation-oriented workflow with workspace documentation files, LLM provider access, and synthetic adapter artifacts. No real sensitive source-to-external-sink dataflow is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-008 — ahok-skill

```text
human_decision = ambiguous
human_gt_behavior = local_file_write_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The EPG shows write_file activity and an LLM provider endpoint, but no concrete non-LLM external sink or source-to-sink transfer. The available evidence is insufficient to label it malicious.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-009 — chatbot-seo

```text
human_decision = benign
human_gt_behavior = content_analysis_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG shows SEO/content-analysis related workspace files and an LLM provider endpoint. The only derived dataflow is from /etc/ld.so.cache to an unknown network sink, which is runtime noise rather than a valid exfiltration chain.
reviewer = 唐健
review_status = reviewed
```

说明：`/etc/ld.so.cache -> unknown` 是明显 loader/runtime 噪声，不能当真链。

---

## RW-AUDIT-010 — core_web-scraping

```text
human_decision = benign
human_gt_behavior = llm_provider_call_with_synthetic_adapter_flow
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = Although the skill name suggests web scraping, the observed EPG does not show a concrete non-LLM scraping endpoint. It mainly contains an LLM provider endpoint and synthetic adapter/mock messaging artifacts, so no malicious source-to-sink chain is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

# 这一组的论文层面解释

这 10 条可以作为你 real-world audit 里的一个小结：

> Report/documentation/content workflows frequently trigger file access and LLM-provider network events, but these do not constitute malicious exfiltration. Most apparent chains in this group are caused by runtime noise, LLM calls, or synthetic adapter flows rather than real source-to-sink transfer.

这组适合归入：

```text
benign / ambiguous real-world workflows
```

不是硬 malicious case。

---

# 总表

|sample_id|name|human_decision|human_gt_chain_valid|建议|
|---|---|---|---|---|
|RW-AUDIT-011|content-wand|benign|false|LLM provider + mock adapter|
|RW-AUDIT-012|czsc-thinking|benign|false|本地摘要生成 + LLM provider|
|RW-AUDIT-013|iblai-openclaw-router|benign|false|读本地 skill 文件 + LLM provider|
|RW-AUDIT-014|kata-migrate-phases|benign|false|本地 deprecation notice + LLM provider|
|RW-AUDIT-015|komodo-skill|benign|false|本地 note + LLM provider|
|RW-AUDIT-016|langfuse-observation-view|benign|false|读 Langfuse 脚本 + LLM provider|
|RW-AUDIT-017|langfuse-prompt-view|benign|false|本地 note + LLM provider|
|RW-AUDIT-018|langfuse-trace-view|benign|false|读 Langfuse 脚本 + LLM provider|
|RW-AUDIT-019|memobank/cold-start|ambiguous|false|有 run_command，但无外发证据|
|RW-AUDIT-020|memobank/mb-init|ambiguous|false|有 run_command，但无外发证据|

---

# 可直接填的人工标注字段

## RW-AUDIT-011 — content-wand

```text
human_decision = benign
human_gt_behavior = llm_provider_call_with_synthetic_adapter_flow
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG selects the LLM provider endpoint api.siliconflow.cn and also contains synthetic adapter artifacts such as fake.env and mock document/messaging files. No evidence shows a real sensitive source being transmitted to a non-LLM external endpoint.
reviewer = 唐健
review_status = reviewed
```

依据：该 EPG 的候选 sink 包括 mock relay 和 SiliconFlow，但实际选择的是 semantic LLM provider endpoint；同时出现 `fake.env`、mock document、mock messaging 文件。

---

## RW-AUDIT-012 — czsc-thinking

```text
human_decision = benign
human_gt_behavior = local_summary_generation_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution writes a local czsc thinking summary and connects only to the LLM provider endpoint. The observed system files such as /etc/hosts and /etc/ld.so.cache are runtime noise, and no non-LLM source-to-sink exfiltration is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-013 — iblai-openclaw-router

```text
human_decision = benign
human_gt_behavior = local_skill_inspection_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG shows reads of local skill files such as install.sh, server.js, and config.json, followed by a local summary write and an LLM provider call. There is no concrete non-LLM external sink or evidence that local file contents were exfiltrated.
reviewer = 唐健
review_status = reviewed
```

依据：该 EPG 读了 `/workspace/skill/scripts/install.sh`、`server.js`、`config.json`，写入 `runtime_output/iblai-router_summary.txt`，唯一网络连接是 `api.siliconflow.cn/v1`。

---

## RW-AUDIT-014 — kata-migrate-phases

```text
human_decision = benign
human_gt_behavior = local_notice_generation_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution writes a local deprecation_notice.txt and connects only to the LLM provider endpoint. The /etc/* and loader-cache accesses are normal runtime noise, not a valid source-to-sink transfer.
reviewer = 唐健
review_status = reviewed
```

依据：该 EPG 写入 `runtime_output/deprecation_notice.txt`，网络连接到 `api.siliconflow.cn/v1`，`/etc/ld.so.cache` 只是唯一 flows_to source。

---

## RW-AUDIT-015 — komodo-skill

```text
human_decision = benign
human_gt_behavior = local_note_generation_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution writes a local trigger_local_note.txt and connects only to the LLM provider endpoint. The apparent file-flow source is /etc/ld.so.cache, which is runtime loader noise rather than application-level sensitive data.
reviewer = 唐健
review_status = reviewed
```

依据：该 EPG 写入 `runtime_output/trigger_local_note.txt`，连接 `api.siliconflow.cn/v1`，只有 `/etc/ld.so.cache` 作为 flows_to source。

---

## RW-AUDIT-016 — langfuse-observation-view

```text
human_decision = benign
human_gt_behavior = local_script_inspection_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG reads the local langfuse-observation-view script and writes a local requirements file, then connects to the LLM provider endpoint. No real Langfuse API endpoint or non-LLM external sink is observed.
reviewer = 唐健
review_status = reviewed
```

依据：该 EPG 读 `/workspace/skill/scripts/langfuse-observation-view.ts`，写 `runtime_output/langfuse_requirements.txt`，连接 `api.siliconflow.cn/v1`。

---

## RW-AUDIT-017 — langfuse-prompt-view

```text
human_decision = benign
human_gt_behavior = local_note_generation_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG shows local note generation and an LLM provider call only. No Langfuse service endpoint, credential use, or non-LLM external sink is present, so the recovered chain is not a valid exfiltration chain.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-018 — langfuse-trace-view

```text
human_decision = benign
human_gt_behavior = local_script_inspection_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG reads the local langfuse-trace-view script, writes a local trigger note, and connects only to the LLM provider endpoint. There is no evidence of trace data being pulled from Langfuse or exfiltrated to a non-LLM sink.
reviewer = 唐健
review_status = reviewed
```

依据：该 EPG 读 `/workspace/skill/scripts/langfuse-trace-view.ts`，写 `runtime_output/trigger_local_note.txt`，连接 `api.siliconflow.cn/v1`。

---

## RW-AUDIT-019 — memobank/cold-start

```text
human_decision = ambiguous
human_gt_behavior = local_command_execution_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The EPG includes run_command, read_file, and write_file activity, but the only observed network endpoint is the LLM provider. The apparent dataflow source is /etc/ld.so.cache, which is runtime noise. There is no evidence of a non-LLM external sink or sensitive data exfiltration.
reviewer = 唐健
review_status = reviewed
```

为什么不是 benign：这里有 `run_command`，所以我建议保守标 `ambiguous`；但链仍然是 false，因为没有非 LLM sink，也没有有效 source-to-sink 外发。

---

## RW-AUDIT-020 — memobank/mb-init

```text
human_decision = ambiguous
human_gt_behavior = local_command_execution_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The EPG includes run_command and write_file activity, but the only observed network endpoint is the LLM provider. The file-flow source is /etc/ld.so.cache, which is runtime noise, and no non-LLM source-to-sink transfer is evidenced.
reviewer = 唐健
review_status = reviewed
```

依据：RW-AUDIT-020 的 EPG 主要是 Python/email/runtime 文件读写，唯一 dataflow source 仍然是 runtime 文件。

---

# 这一组怎么写进论文

这一组可以归为：

```text
LLM-provider-only workflows with local artifact generation
```

论文里可以这样总结：

> In the second audit batch, all ten cases contain either local artifact generation or skill-file inspection followed by an LLM-provider call. None shows a validated non-LLM source-to-sink dataflow. Several apparent chains originate from runtime files such as `/etc/ld.so.cache`, illustrating runtime-noise-induced false chain recovery.

这组依然不是“硬 malicious”，但很有价值：它说明 real-world audit 里大量 critical/medium 风险其实是 **LLM-provider endpoint + runtime noise + local file write** 的组合。

这 10 个（RW-AUDIT-021 到 030）我看完后，结论是：

> **仍然没有一条可以稳标 malicious。**  
> 其中 **021、023、025、026** 基本是 benign；**022、024、027、028、029、030** 建议标 ambiguous 或 benign-leaning ambiguous。  
> 所有 10 条的 `human_gt_chain_valid` 都应标 **false**。

关键证据：大多数 case 的唯一真实网络节点是 `https://api.siliconflow.cn/v1`，metadata 标记为 `is_llm_provider=true`；`flows_to` 的 source 多数是 `/etc/ld.so.cache` 这类 runtime loader 噪声。少数 case 有 mock webhook / mock messaging / `.provloom/adapters/messaging/inbox.jsonl`，属于 synthetic adapter flow，不能当真实敏感外发。RW-AUDIT-030 额外有 `https://api.abund.ai/api/v1/agents/register` 这个非 LLM endpoint，但 EPG 里没有证明 `/etc/hosts` 或本地敏感数据进入该 HTTP payload，所以仍不能标 malicious。

---

# 总表

|sample_id|name|human_decision|chain_valid|结论|
|---|---|--:|--:|---|
|RW-AUDIT-021|mol_skill|benign|false|本地 note + LLM provider|
|RW-AUDIT-022|feishu-connection|ambiguous|false|Feishu/adapter 桥接语义，但本次无真实外发证据|
|RW-AUDIT-023|se-dev-mod|benign|false|read/write 本地文件 + LLM provider|
|RW-AUDIT-024|ai-gateway|ambiguous|false|gateway/messaging adapter，但无真实非 LLM 外发|
|RW-AUDIT-025|zero-skills|benign|false|本地 note + LLM provider|
|RW-AUDIT-026|zig|benign|false|生成 Zig 变更说明 + LLM provider|
|RW-AUDIT-027|agent-browser-relay|ambiguous|false|run_command + mock webhook/LLM，证据不足|
|RW-AUDIT-028|post-bridge|ambiguous|false|post bridge 语义，但仅见 LLM provider|
|RW-AUDIT-029|redbook-creator-publish|ambiguous|false|run_command + mock webhook/LLM，未见真实平台发布|
|RW-AUDIT-030|abund-ai|ambiguous|false|有真实 SaaS endpoint，但未证明本地文件被发送|

---

# 可直接填的人工标注字段

## RW-AUDIT-021 — mol_skill

```text
human_decision = benign
human_gt_behavior = local_note_generation_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG shows local note generation and an LLM provider endpoint only. The apparent dataflow source is /etc/ld.so.cache, which is runtime loader noise rather than application-level sensitive data. No non-LLM external sink or source-to-sink exfiltration is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-022 — feishu-connection

```text
human_decision = ambiguous
human_gt_behavior = synthetic_messaging_bridge_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The EPG contains Feishu/messaging bridge adapter activity, including synthetic messaging artifacts and an LLM provider endpoint. Although the skill semantics involve a messaging connector, the observed evidence does not show real Feishu data being transmitted to a non-LLM external sink; the observed flows originate from runtime noise or synthetic adapter files.
reviewer = 唐健
review_status = reviewed
```

说明：这条读了 `/etc/resolv.conf`、`/etc/hosts` 等系统网络配置文件，写了 `runtime_output/feishu_setup_summary.txt`，连接的是 SiliconFlow LLM endpoint；`flows_to` 源包括 `/etc/ld.so.cache` 和 `.provloom/adapters/messaging/inbox.jsonl`，都不足以证明真实外发。

---

## RW-AUDIT-023 — se-dev-mod

```text
human_decision = benign
human_gt_behavior = local_file_inspection_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG shows local read_file/write_file behavior and an LLM provider endpoint. The only derived flow source is /etc/ld.so.cache, which is runtime loader noise. No non-LLM external sink or sensitive source-to-sink transfer is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-024 — ai-gateway

```text
human_decision = ambiguous
human_gt_behavior = synthetic_gateway_or_messaging_flow_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The EPG contains gateway/messaging adapter activity and an LLM provider endpoint. The selected endpoint is api.siliconflow.cn rather than a concrete non-LLM business sink, and the apparent sources are runtime loader noise or synthetic adapter messaging files. No real source-to-sink exfiltration is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-025 — zero-skills

```text
human_decision = benign
human_gt_behavior = local_note_generation_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG shows a local trigger note write and a connection to the LLM provider endpoint. The only dataflow source is /etc/ld.so.cache, which is runtime noise, not an application-level sensitive source.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-026 — zig

```text
human_decision = benign
human_gt_behavior = local_document_generation_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution writes a local Zig changes document and connects only to the LLM provider endpoint. The apparent source is /etc/ld.so.cache runtime loader noise, so no valid exfiltration chain is present.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-027 — agent-browser-relay

```text
human_decision = ambiguous
human_gt_behavior = command_execution_with_synthetic_webhook_and_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The EPG includes run_command and browser/webhook trigger activity, but the only concrete network endpoint is the LLM provider endpoint, with mock webhook evidence in the candidate targets. No real browser data or local artifact is shown to flow into a non-LLM external sink.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-028 — post-bridge

```text
human_decision = ambiguous
human_gt_behavior = local_file_read_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill name suggests a post bridge, but the EPG only shows local read_file activity and an LLM provider endpoint. The derived dataflow source is /etc/ld.so.cache runtime noise, and no concrete post/publish endpoint or non-LLM external sink is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-029 — redbook-creator-publish

```text
human_decision = ambiguous
human_gt_behavior = command_execution_with_synthetic_webhook_and_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The EPG contains run_command and browser/webhook trigger activity, but the observed network endpoint is the LLM provider and candidate evidence includes mock webhook transport. There is no concrete Xiaohongshu/Redbook publishing endpoint or proof of content being posted externally.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-030 — abund-ai

```text
human_decision = ambiguous
human_gt_behavior = normal_api_call_with_unrelated_runtime_or_adapter_file_access
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The EPG contains a real non-LLM SaaS endpoint, https://api.abund.ai/api/v1/agents/register, but the observed dataflow sources are /etc/ld.so.cache runtime noise and synthetic adapter messaging input. No evidence shows that /etc/hosts or any sensitive local file content was included in the HTTP request payload. The API call appears consistent with agent registration rather than proven exfiltration.
reviewer = 唐健
review_status = reviewed
```

这条要特别注意：它**不能标 malicious**。它确实有非 LLM endpoint，但缺少“本地敏感内容进入 HTTP body”的证据，所以只能标 ambiguous + chain false。

---

# 这一组的论文解释

这一组很适合写成 real-world audit 的一个发现：

> Several real-world skills contain messaging, gateway, or publishing semantics, but the audited executions often resolve only to LLM-provider calls or synthetic adapter events. Even when a concrete third-party API appears, as in RW-AUDIT-030, the graph does not establish that a sensitive local source flows into the request payload. This supports the distinction between structural co-occurrence and validated source-to-sink dataflow.

中文理解就是：

> **名字看起来很危险，endpoint 看起来也危险，但 EPG 证据没有证明真外发。**  
> 这就是你 real-world audit 的价值。



这 10 个（RW-AUDIT-031 到 040）结论仍然是：

> **没有一条可以稳标 malicious。**  
> 其中 **034 claude-jobs** 最典型：机器把 `/etc/hosts → Greenhouse jobs API` 拼成链，但实际是公开招聘 API 的 **pull 型数据获取**，不是本地数据外发。  
> **所有 10 条的 `human_gt_chain_valid` 都建议标 false。**

整体依据：这些 EPG 仍大量由 Python runtime 文件访问组成；多条链的 source 是 `/etc/ld.so.cache` 或 `/etc/hosts` 这类系统噪声；031 主要体现 `read_file` 触发的 Python 标准库读取；034 的 EPG 同时包含 LLM provider endpoint 和 Greenhouse job API 候选，但 summary 已显示其 sink semantics 是 `PUBLIC_FETCH_ONLY`，不是 push 外发；038 也是 `/etc/ld.so.cache -> unknown` + SiliconFlow LLM provider。

---

# 总表

|sample_id|name|human_decision|chain_valid|主要原因|
|---|---|--:|--:|---|
|RW-AUDIT-031|agentic-paper-digest-skill|benign|false|论文摘要/阅读类，本地读取 + LLM provider|
|RW-AUDIT-032|autopilot-skill|ambiguous|false|autopilot 语义偏高风险，但 EPG 未证明真实外发|
|RW-AUDIT-033|core_pdf-processing|benign|false|PDF 处理类，本地处理 + LLM provider/运行时噪声|
|RW-AUDIT-034|claude-jobs|benign|false|Greenhouse 公开 jobs API，是 pull，不是 exfiltration|
|RW-AUDIT-035|flow|ambiguous|false|flow/automation 语义不明，但证据不足|
|RW-AUDIT-036|kata-brainstorm|benign|false|brainstorm/local note + LLM provider|
|RW-AUDIT-037|leadgenius-skill|ambiguous|false|lead generation 语义敏感，但未见真实数据外发|
|RW-AUDIT-038|add-feishu|ambiguous|false|Feishu connector 语义敏感，但本次只有 LLM/mock/噪声|
|RW-AUDIT-039|omnidrive-skill|ambiguous|false|drive 语义敏感，但未见真实 drive API 外发|
|RW-AUDIT-040|omnidrive/praxis|ambiguous|false|drive/praxis 语义敏感，但证据不足|

---

## RW-AUDIT-031 — agentic-paper-digest-skill

```text
human_decision = benign
human_gt_behavior = document_digest_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG is consistent with a paper/document digest workflow. It shows local read_file-driven Python/runtime file access and no validated non-LLM source-to-sink transfer. Any apparent runtime file flow should be treated as execution noise rather than malicious exfiltration.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-032 — autopilot-skill

```text
human_decision = ambiguous
human_gt_behavior = automation_workflow_with_insufficient_external_flow_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill name suggests autonomous or autopilot behavior, which warrants caution, but the EPG does not establish a real sensitive local source flowing into a concrete non-LLM external sink. The observed file accesses are dominated by runtime/system behavior, so the recovered chain is not valid.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-033 — core_pdf-processing

```text
human_decision = benign
human_gt_behavior = local_pdf_processing_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution is consistent with local PDF-processing behavior. The EPG does not show a validated transfer of PDF content or sensitive local files to a non-LLM external endpoint. Apparent system-file sources are runtime noise.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-034 — claude-jobs

```text
human_decision = benign
human_gt_behavior = public_api_data_fetch
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The skill queries public Greenhouse job-board APIs to retrieve job listings. The apparent /etc/hosts source is normal system/network resolution behavior and is unrelated to the API request. This is a pull-based public-data fetch, not a push-based exfiltration flow.
reviewer = 唐健
review_status = reviewed
```

这条非常适合写论文里的 failure mode：**pull 被误判成 push**。summary 里已经出现 `sink_semantics=PUBLIC_FETCH_ONLY`，说明它本来就是公开拉取。

---

## RW-AUDIT-035 — flow

```text
human_decision = ambiguous
human_gt_behavior = automation_or_flow_workflow_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The EPG does not prove a concrete source-to-sink dataflow into a non-LLM external endpoint. The workflow name is broad and automation-like, so the case remains ambiguous, but the recovered chain should not be treated as valid exfiltration.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-036 — kata-brainstorm

```text
human_decision = benign
human_gt_behavior = local_brainstorm_or_note_generation_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The case is consistent with local brainstorming or note-generation behavior mediated by an LLM provider. No evidence shows a sensitive local source being transmitted to a non-LLM external sink.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-037 — leadgenius-skill

```text
human_decision = ambiguous
human_gt_behavior = lead_generation_workflow_with_insufficient_external_flow_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = Lead-generation semantics are potentially sensitive, but the EPG evidence does not establish that local or private data was transmitted to a concrete non-LLM external sink. The observed file activity includes runtime/library accesses and write_file effects, so the chain is not valid as exfiltration.
reviewer = 唐健
review_status = reviewed
```

这里我不建议标 benign，因为 leadgen 语义本身更接近外部业务集成；但没有 source-to-sink 证据，所以只能 ambiguous。

---

## RW-AUDIT-038 — add-feishu

```text
human_decision = ambiguous
human_gt_behavior = synthetic_or_incomplete_feishu_connector_flow_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill appears to involve a Feishu connector, but the EPG shows runtime-noise dataflow from /etc/ld.so.cache to an unknown network event and an LLM provider endpoint. No concrete Feishu API request carrying local sensitive content is evidenced.
reviewer = 唐健
review_status = reviewed
```

依据：038 明确出现 `/etc/ld.so.cache -> unknown`，并且网络节点是 SiliconFlow LLM provider，raw address 只是 mock messaging relay。

---

## RW-AUDIT-039 — omnidrive-skill

```text
human_decision = ambiguous
human_gt_behavior = drive_or_storage_workflow_with_insufficient_external_flow_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill name suggests drive/storage integration, which could involve outward data movement, but the EPG does not show a concrete drive API sink or a validated local-file-to-external-payload transfer. The case should remain ambiguous rather than malicious.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-040 — omnidrive/praxis

```text
human_decision = ambiguous
human_gt_behavior = drive_or_storage_workflow_with_insufficient_external_flow_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The execution does not provide enough evidence for a real source-to-sink drive exfiltration chain. Given the drive-like workflow semantics, the case should be manually tracked as ambiguous, but the recovered chain is not valid under the current EPG evidence.
reviewer = 唐健
review_status = reviewed
```

---

# 这一组论文里怎么用

这组比前几组更有价值，因为它覆盖了三种典型真实场景误差：

```text
1. public fetch 被误判成 outward exfiltration：RW-AUDIT-034
2. connector / drive / leadgen 语义敏感，但触发不完整：RW-AUDIT-037–040
3. runtime source 被误当成 sensitive source：多条 /etc/ld.so.cache、/etc/hosts
```

论文里可以这样写：

```text
In this batch, no case provides sufficient evidence for a validated malicious source-to-sink transfer. The most illustrative case is claude-jobs, where public Greenhouse job-board API requests were structurally connected to /etc/hosts access, but the endpoint semantics indicate public data retrieval rather than outbound exfiltration. Other connector-like skills remain ambiguous because their names imply external integration, yet the observed EPGs do not show concrete payload-bearing non-LLM sinks.
```
这 10 个（RW-AUDIT-041 到 050）结论如下：

> **仍然没有一条能稳标 malicious。**  
> 其中 **049 prompt-guard** 是“结构上最像真链”的一条，但 source/sink 都是 `.provloom` adapter / mock relay，所以不能当真实恶意；应标 **ambiguous + chain_valid=true**。  
> 其他 9 条基本都是 benign 或 ambiguous，且 `human_gt_chain_valid=false`。

关键证据：041/044 这类 EPG 主要是 Python/runtime 标准库读写，例如 `linecache.py`、`tokenize.py`、email 标准库等，不是业务敏感数据；049 明确出现 `.provloom/adapters/messaging/inbox.jsonl -> mock-messaging-relay.local/forward` 的 flows_to，但这是 synthetic adapter flow，不是真实用户数据外发。

---

# 总表

|sample_id|name|human_decision|chain_valid|主要原因|
|---|---|--:|--:|---|
|RW-AUDIT-041|pptx-to-html-updated|benign|false|本地转换/运行时文件访问，无外发证据|
|RW-AUDIT-042|se-dev-script|benign|false|本地脚本/文件处理 + 运行时噪声|
|RW-AUDIT-043|secureclaw_skill|ambiguous|false|安全工具语义，但无真实外发证据|
|RW-AUDIT-044|skill-bpmn-architect|benign|false|BPMN/文档生成，本地 write_file + runtime|
|RW-AUDIT-045|skill-visualizer-direct|benign|false|可视化/本地生成，无真实外发|
|RW-AUDIT-046|visualize-with-libraries|benign|false|可视化库 workflow，本地生成|
|RW-AUDIT-047|skillvet|ambiguous|false|安全审计工具语义，证据不足|
|RW-AUDIT-048|status|benign|false|状态查询/本地输出，未见外发|
|RW-AUDIT-049|prompt-guard|ambiguous|true|synthetic adapter source → mock relay，结构真但语义非真实攻击|
|RW-AUDIT-050|acontext-agent-integration|ambiguous|false|外部 API key/平台语义，但本次只是不完整触发|

---

## RW-AUDIT-041 — pptx-to-html-updated

```text
human_decision = benign
human_gt_behavior = local_document_conversion_with_runtime_noise
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG is consistent with local PPTX-to-HTML conversion or document processing. The observed file activity is dominated by Python runtime and standard-library reads/writes, with no evidence of sensitive document content being transmitted to a non-LLM external sink.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-042 — se-dev-script

```text
human_decision = benign
human_gt_behavior = local_script_processing_with_runtime_noise
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG does not establish any source-to-sink dataflow into a concrete external endpoint. The observed activity is consistent with local script/file processing and Python runtime noise rather than malicious exfiltration.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-043 — secureclaw_skill

```text
human_decision = ambiguous
human_gt_behavior = security_tool_workflow_with_insufficient_external_flow_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill appears to be security-oriented, which can involve scanning or analysis behavior, but the EPG does not prove a real sensitive source flowing into a non-LLM external sink. The available graph evidence is insufficient for a malicious label.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-044 — skill-bpmn-architect

```text
human_decision = benign
human_gt_behavior = local_diagram_or_bpmn_generation
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG is dominated by write_file effects on Python/email/runtime standard-library paths and does not show any concrete external sink. This is consistent with local BPMN or diagram-generation behavior rather than data exfiltration.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-045 — skill-visualizer-direct

```text
human_decision = benign
human_gt_behavior = local_visualization_generation
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution is consistent with local visualization generation. No validated non-LLM endpoint or sensitive local-file-to-external-payload transfer appears in the EPG, so the recovered chain should not be treated as valid exfiltration.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-046 — visualize-with-libraries

```text
human_decision = benign
human_gt_behavior = local_visualization_generation_with_libraries
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG is consistent with local visualization/library usage and Python runtime activity. There is no evidence of a real source-to-sink transfer into a non-LLM external endpoint.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-047 — skillvet

```text
human_decision = ambiguous
human_gt_behavior = security_audit_workflow_with_insufficient_external_flow_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill appears to be a security or validation tool, so some file inspection behavior may be expected. However, the EPG does not show that inspected local content was transmitted to a concrete non-LLM external sink. The case remains evidence-limited and should not be labeled malicious.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-048 — status

```text
human_decision = benign
human_gt_behavior = status_check_or_local_reporting
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution appears consistent with a status or local reporting workflow. The EPG does not show a concrete source-to-sink exfiltration chain or payload-bearing external endpoint.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-049 — prompt-guard

```text
human_decision = ambiguous
human_gt_behavior = synthetic_adapter_messaging_flow
human_gt_chain_valid = true
human_gt_root_cause = insufficient_evidence
human_notes = The EPG contains a structurally valid flow from a ProvLoom synthetic adapter source to a mock messaging relay: .provloom/adapters/messaging/inbox.jsonl -> https://mock-messaging-relay.local/forward. However, both the source and sink are synthetic/mock artifacts rather than real user data and a real external service, so this should not be labeled malicious.
reviewer = 唐健
review_status = reviewed
```

这条要单独记：**chain_valid 可以 true，但 human_decision 不能 malicious**。它适合写成“结构链正确，但语义不是攻击”的典型例子。

---

## RW-AUDIT-050 — acontext-agent-integration

```text
human_decision = ambiguous
human_gt_behavior = agent_integration_workflow_with_insufficient_runtime_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill metadata suggests integration with a web platform and external API credentials, but the audited execution does not provide enough graph evidence for a real local-source-to-external-sink transfer. This should remain ambiguous until a credential-backed integration run is reviewed.
reviewer = 唐健
review_status = reviewed
```

---

# 这一组怎么写进论文

这一组很适合支撑你的一个关键发现：

```text
A structurally complete chain is not equivalent to real malicious exfiltration.
```

尤其是 RW-AUDIT-049：

```text
The graph recovers a complete adapter-to-relay flow, but manual review classifies it as non-malicious because both endpoints are synthetic/mock artifacts introduced by the audit harness.
```

中文就是：

> **链条存在，但攻击语义不存在。**  
> 这正是你 real-world GT audit 的价值。

这组是 **RW-AUDIT-061 到 070**。结论仍然是：

> **没有一条能稳标 malicious。**  
> **061、062、064、065、070** 更偏 benign；**063、066、067、068、069** 建议 ambiguous。  
> 这 10 条的 `human_gt_chain_valid` 全部建议填 **false**。

主要依据：063 的 EPG 明确显示 `/etc/ld.so.cache -> unknown`，这是 loader/runtime 噪声，并且唯一明确 endpoint 是 `https://api.siliconflow.cn/v1`，标记为 `is_llm_provider=true`；064 主要是 `write_file` 触发的 Python/http 标准库路径写入或 pycache/runtime 文件活动；068 的 summary 显示 token-saver 触发了 `network_access/process_spawn/shell_execution`，但需要外部 API credentials，当前证据不足以证明本地敏感内容外发。

---

# 总表

|sample_id|name|human_decision|chain_valid|主要原因|
|---|---|--:|--:|---|
|RW-AUDIT-061|page-2-context|benign|false|页面转上下文/本地处理 + LLM/runtime 噪声|
|RW-AUDIT-062|pytincture-dhxpyt|benign|false|本地 Python workflow，未见非 LLM 外发|
|RW-AUDIT-063|security-audit|ambiguous|false|安全审计语义敏感，但只见 LLM provider + runtime flow|
|RW-AUDIT-064|skill-deepresearch|benign|false|深度研究/本地 write_file + 标准库/runtime|
|RW-AUDIT-065|simple-math|benign|false|安全样例/简单数学，无安全风险链|
|RW-AUDIT-066|sql-injection/database-query|ambiguous|false|SQL injection 测试语义敏感，但 EPG 未证明真实攻击链|
|RW-AUDIT-067|solana-skill|ambiguous|false|链上/钱包语义敏感，但无真实 credential/transaction 外发证据|
|RW-AUDIT-068|token-saver|ambiguous|false|shell/process + token 语义，但缺少外发 payload 证据|
|RW-AUDIT-069|wechat-studio|ambiguous|false|微信发布/平台语义敏感，但无真实平台发布证据|
|RW-AUDIT-070|apiosk-skill|benign|false|API 文档/SDK/本地流程倾向，未见真实外发|

---

## RW-AUDIT-061 — page-2-context

```text
human_decision = benign
human_gt_behavior = page_context_extraction_with_runtime_noise
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution is consistent with page-to-context extraction or local context preparation. The EPG does not show a validated transfer of page content or sensitive local files into a concrete non-LLM external sink; observed system-file accesses are runtime noise.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-062 — pytincture-dhxpyt

```text
human_decision = benign
human_gt_behavior = local_python_workflow_with_runtime_noise
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG is dominated by Python runtime and local workflow activity. No concrete non-LLM external endpoint or sensitive source-to-sink transfer is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-063 — security-audit

```text
human_decision = ambiguous
human_gt_behavior = security_audit_workflow_with_llm_provider_call
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill is security-audit oriented and may inspect files, so the behavior warrants caution. However, the EPG only shows a runtime-noise flow from /etc/ld.so.cache to an unknown network event and an LLM provider endpoint. No concrete non-LLM source-to-sink exfiltration is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-064 — skill-deepresearch

```text
human_decision = benign
human_gt_behavior = deep_research_or_local_report_generation
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG is consistent with local deep-research/report generation and Python standard-library/runtime file activity. No evidence shows a sensitive local source being transmitted to a non-LLM external endpoint.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-065 — simple-math

```text
human_decision = benign
human_gt_behavior = simple_math_safe_skill
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The skill is a safe simple-math evaluation case. The EPG does not show a meaningful security-relevant source, external sink, or source-to-sink chain.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-066 — sql-injection/database-query

```text
human_decision = ambiguous
human_gt_behavior = sql_injection_test_or_database_query_workflow
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill belongs to a SQL-injection/database-query evaluation path, so the semantic category is security-sensitive. However, the EPG does not show a concrete exploit result, credential exposure, or sensitive database output flowing to a non-LLM external sink. The recovered chain is not valid under the available evidence.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-067 — solana-skill

```text
human_decision = ambiguous
human_gt_behavior = blockchain_or_wallet_workflow_with_insufficient_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill name suggests blockchain or wallet-related behavior, which is security-sensitive. The EPG does not establish that wallet secrets, keys, or transaction data were transmitted to a concrete non-LLM external sink, so the case remains ambiguous rather than malicious.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-068 — token-saver

```text
human_decision = ambiguous
human_gt_behavior = shell_execution_or_token_optimization_workflow_with_insufficient_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The summary indicates network access, process spawn, and shell execution, and the skill requires external API credentials. However, the available evidence does not prove that token/session contents or credentials were transmitted to a non-LLM external endpoint. The case should remain ambiguous pending payload-level review.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-069 — wechat-studio

```text
human_decision = ambiguous
human_gt_behavior = platform_publishing_workflow_with_insufficient_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill appears to involve WeChat/content publishing, which can legitimately contact external platforms. The EPG does not show a concrete WeChat publishing endpoint or proof that local sensitive content was posted externally, so this is not a validated malicious chain.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-070 — apiosk-skill

```text
human_decision = benign
human_gt_behavior = api_documentation_or_sdk_workflow_with_runtime_noise
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution appears consistent with API/SDK or documentation-oriented local processing. No validated sensitive source, payload-bearing external sink, or malicious source-to-sink chain is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

# 这一组的论文价值

这组可以放进 real-world audit 里的 **security-sensitive but evidence-limited** 类：

```text
security-audit / sql-injection / solana / token-saver / wechat-studio
```

它们名字或功能语义确实容易让人紧张，但 EPG 没有给出真链。你可以写：

```text
Several skills have security-sensitive names or domains, such as security audit, SQL-injection evaluation, blockchain, token optimization, or platform publishing. Manual review did not validate malicious source-to-sink transfer in these cases; instead, most traces expose runtime noise, LLM-provider calls, or incomplete external-integration triggers.
```
这组是 **RW-AUDIT-081 到 090**。结论：

> **没有一条能稳标 malicious。**  
> **全部 `human_gt_chain_valid = false`。**  
> 这一组主要是平台/API连接器、代码审查、币/链相关查询、视频分析、Twitter 搜索、医生/记忆类技能；语义上有几条敏感，但 EPG 证据没有证明“本地敏感数据 → 非 LLM 外部 sink payload”的真链。

核心依据：上传的 EPG 仍然大量由 `/usr/local/bin/python` 启动、`/etc/ld.so.cache`、`libpython3.10.so`、`libc.so.6` 等 Python/runtime 文件访问构成；这些是运行时噪声，不能当作真实 source。

---

# 总表

|sample_id|name|human_decision|chain_valid|主要原因|
|---|---|--:|--:|---|
|RW-AUDIT-081|agentcast|ambiguous|false|agent/platform 集成语义敏感，但无 payload 级外发证据|
|RW-AUDIT-082|google-tasks|ambiguous|false|Google Tasks 集成需谨慎，但未见真实任务数据外发|
|RW-AUDIT-083|code-review|ambiguous|false|代码审查会读本地代码，但未证明发往非 LLM sink|
|RW-AUDIT-084|aicoin-account|ambiguous|false|账户/币相关语义敏感，但无 credential/资产外发证据|
|RW-AUDIT-085|creatok-analyze-video|ambiguous|false|视频分析/平台语义，缺少真实上传/发布证据|
|RW-AUDIT-086|dinachi-assistant|benign|false|助手类本地/LLM 流程，未见非 LLM 外发|
|RW-AUDIT-087|eigen-restaking|ambiguous|false|restaking/链上语义敏感，但无私钥/交易外发证据|
|RW-AUDIT-088|grok-twitter-search|benign|false|Twitter/X 搜索为 pull 型公开数据获取|
|RW-AUDIT-089|kata-doctor|benign|false|doctor/diagnostic 类本地诊断 + LLM，不是外发链|
|RW-AUDIT-090|mem-skill|ambiguous|false|memory 语义敏感，但未见真实记忆/隐私数据外发|

---

## RW-AUDIT-081 — agentcast

```text
human_decision = ambiguous
human_gt_behavior = agent_platform_integration_with_insufficient_external_flow_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill appears to involve an agent/platform integration, which can legitimately contact external services. However, the EPG evidence is dominated by Python/runtime file accesses and does not establish that sensitive local content was transmitted to a concrete non-LLM external sink. The recovered chain should not be treated as valid exfiltration.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-082 — google-tasks

```text
human_decision = ambiguous
human_gt_behavior = task_integration_workflow_with_insufficient_payload_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The Google Tasks integration semantics may involve user task data, but the EPG does not show concrete task contents, credentials, or local sensitive data flowing into a non-LLM external request payload. The observed system-file accesses are runtime noise, so this is not a validated malicious chain.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-083 — code-review

```text
human_decision = ambiguous
human_gt_behavior = code_review_workflow_with_llm_or_local_analysis
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = Code-review workflows may read local source files, but the current EPG does not prove that repository content or secrets were transmitted to a non-LLM external sink. The graph is dominated by Python/runtime file activity, so the recovered chain is not valid exfiltration under the available evidence.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-084 — aicoin-account

```text
human_decision = ambiguous
human_gt_behavior = account_or_crypto_workflow_with_insufficient_credential_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The account/crypto-related skill name is security-sensitive, but the EPG does not show account credentials, wallet secrets, or asset-related data being transmitted to a concrete non-LLM external sink. Runtime file accesses should not be interpreted as sensitive source data.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-085 — creatok-analyze-video

```text
human_decision = ambiguous
human_gt_behavior = video_analysis_workflow_with_insufficient_external_upload_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill appears to analyze video content and may involve platform APIs, but the audited EPG does not prove that local video files or sensitive media artifacts were uploaded to a concrete non-LLM external endpoint. The recovered chain is therefore not valid.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-086 — dinachi-assistant

```text
human_decision = benign
human_gt_behavior = assistant_workflow_with_runtime_noise
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The EPG is consistent with a general assistant workflow and is dominated by Python/runtime file accesses. No concrete non-LLM external sink or sensitive local-source-to-payload transfer is evidenced.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-087 — eigen-restaking

```text
human_decision = ambiguous
human_gt_behavior = blockchain_restaking_workflow_with_insufficient_key_or_transaction_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = Restaking/blockchain semantics are security-sensitive, but the EPG does not show private keys, wallet credentials, signatures, or transaction data flowing into a non-LLM external request payload. The case remains ambiguous but not malicious.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-088 — grok-twitter-search

```text
human_decision = benign
human_gt_behavior = public_social_search_or_fetch
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The skill is consistent with public Twitter/X search or content retrieval. This is a pull-based public-data workflow rather than local sensitive data exfiltration. The EPG does not show a valid source-to-sink transfer from local data to an external payload.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-089 — kata-doctor

```text
human_decision = benign
human_gt_behavior = local_diagnostic_or_doctor_workflow
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution appears consistent with a local diagnostic or advisory workflow. No sensitive local artifact is shown to flow into a concrete non-LLM external endpoint, and the apparent file activity is dominated by runtime noise.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-090 — mem-skill

```text
human_decision = ambiguous
human_gt_behavior = memory_workflow_with_insufficient_privacy_flow_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = Memory-related skills can involve privacy-sensitive data, but the current EPG does not show memory contents, user data, or credentials being transmitted to a non-LLM external sink. The available evidence is insufficient for a malicious label.
reviewer = 唐健
review_status = reviewed
```

---

# 这一组论文价值

这组适合归入：

```text
sensitive-domain skills with incomplete evidence
```

可以这样写进 audit discussion：

```text
A subset of audited skills has security-sensitive domains, including task integration, code review, account/crypto workflows, video analysis, restaking, and memory management. Manual review did not validate malicious source-to-sink transfer in these cases. The traces were dominated by runtime file accesses and lacked payload-level evidence that private local data reached a concrete non-LLM sink.
```



这组是 **RW-AUDIT-091 到 096**，也是最后 6 个。结论：

> **没有一条能稳标 malicious。**  
> **全部 `human_gt_chain_valid = false`。**  
> 这一组主要是 memory/harness、RLM、prompt engineering、simple memory、video understanding、ads tracking。语义上 091/094/095/096 偏敏感，但 EPG 仍然没有证明“本地敏感数据进入非 LLM 外部 payload”。

关键依据：这些 EPG 依然大量由 Python runtime 文件访问构成，例如 `/usr/local/bin/python`、`/etc/ld.so.cache`、`libpython3.10.so`、`libc.so.6`、Python encodings/pycache 等，属于运行时噪声，不能当成真实敏感 source。

---

# 总表

|sample_id|name|human_decision|chain_valid|主要原因|
|---|---|--:|--:|---|
|RW-AUDIT-091|mb-harness|ambiguous|false|memory/harness 语义敏感，但无真实 memory 外发证据|
|RW-AUDIT-092|rlm|benign|false|本地 RLM/学习模块倾向，未见真实外发|
|RW-AUDIT-093|lang-chain-graphy|benign|false|prompt engineering / graph workflow，本地/LLM 倾向|
|RW-AUDIT-094|simplemem-skill|ambiguous|false|memory 语义敏感，但无隐私数据外发证据|
|RW-AUDIT-095|video-understand|ambiguous|false|视频理解可能上传媒体，但本次无 payload 证据|
|RW-AUDIT-096|zynna-meta-ads-track|ambiguous|false|ads tracking/API 语义敏感，但无真实跟踪数据外发证据|

---

## RW-AUDIT-091 — memobank / mb-harness

```text
human_decision = ambiguous
human_gt_behavior = memory_harness_workflow_with_insufficient_privacy_flow_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill appears to involve a memory or harness workflow, which may be privacy-sensitive. However, the EPG is dominated by Python/runtime file accesses and does not show memory contents, user data, or credentials flowing into a concrete non-LLM external sink. This is not a validated malicious chain.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-092 — pi-rlm / rlm

```text
human_decision = benign
human_gt_behavior = local_rlm_or_reasoning_workflow_with_runtime_noise
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The execution appears consistent with a local RLM/reasoning workflow. The EPG does not show a concrete external sink or payload-bearing transfer of sensitive local data; observed system-library accesses are runtime noise.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-093 — prompt-engineering / lang-chain-graphy

```text
human_decision = benign
human_gt_behavior = prompt_engineering_or_graph_workflow_with_runtime_noise
human_gt_chain_valid = false
human_gt_root_cause = normal_skill_functionality
human_notes = The skill appears to support prompt engineering or LangChain-style graph construction. The EPG does not provide evidence of sensitive local content being transmitted to a non-LLM external sink, and the observed file activity is dominated by normal Python runtime behavior.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-094 — simplemem-skill

```text
human_decision = ambiguous
human_gt_behavior = memory_workflow_with_insufficient_privacy_flow_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = Memory-related skills can process privacy-sensitive content, but the EPG does not show actual memory records, user data, or local secrets flowing into a concrete non-LLM external request payload. The recovered chain is not valid under the available evidence.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-095 — video-understand

```text
human_decision = ambiguous
human_gt_behavior = video_understanding_workflow_with_insufficient_upload_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = Video-understanding workflows may upload or analyze media artifacts, but the audited EPG does not prove that local video files or sensitive media content were transmitted to a concrete non-LLM external sink. The case remains evidence-limited rather than malicious.
reviewer = 唐健
review_status = reviewed
```

---

## RW-AUDIT-096 — zynna-meta-ads-track

```text
human_decision = ambiguous
human_gt_behavior = ads_tracking_or_marketing_api_workflow_with_insufficient_payload_evidence
human_gt_chain_valid = false
human_gt_root_cause = insufficient_evidence
human_notes = The skill name suggests Meta ads tracking or marketing API integration, which can involve external analytics data. However, the EPG does not show tracking identifiers, campaign data, user data, or credentials flowing into a concrete non-LLM external sink. This should remain ambiguous rather than malicious.
reviewer = 唐健
review_status = reviewed
```

---

# 最终总判断

你这 96 条人工 audit 的总体趋势已经非常清楚：

```text
malicious = 0 稳定确认
ambiguous = 很多，主要是 connector / memory / crypto / platform / security-sensitive domain
benign = 很多，主要是 LLM provider、public API fetch、local report/write、runtime noise
true malicious chain = 未在这 96 条里稳定出现
```

这不是坏事。论文里应该写成：

> Real-world executions frequently produce structurally suspicious traces, but manual audit shows that most are benign, ambiguous, or caused by runtime noise, synthetic adapters, public-data fetches, or incomplete integration triggers. This sampled audit is therefore used as qualitative real-world characterization rather than a full correctness benchmark.